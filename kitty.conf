# Golden Kitty Config
#
# Single entrypoint for both Linux and macOS:
# 1) shared defaults
# 2) OS-specific overrides via KITTY_OS (linux|macos)
# 3) shared theme

include                              ./common.conf
include                              ./${KITTY_OS}.conf
include                              ./theme.conf
globinclude                          ./local.conf
